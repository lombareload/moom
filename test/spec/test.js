/* global describe, it */
var assert = require('assert');

(function () {
  'use strict';

  describe('Queue', function () {
    describe('should be empty by default', function () {
      it('should run here few assertions', function () {
        assert.true(false);
      });
    });
  });
})();
